# Dependencies for the Streamlit deployment (Streamlit Community Cloud reads this file
# from the repo root; also used to build the local dev venv).
# NOTE: requires Python 3.12 (set it in the Streamlit Cloud "Advanced settings"). These
# pins have no wheels for Python 3.13+/3.14.
# CPU-only PyTorch keeps the install small — inference needs no GPU. The explicit +cpu
# local version forces the CPU wheel from the index below (never the large CUDA build).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

streamlit==1.36.0
grad-cam==1.5.0
pillow==10.3.0
numpy==1.26.4
pandas==2.2.2
opencv-python-headless==4.9.0.80
